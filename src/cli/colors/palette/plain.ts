import { Color, Palette } from '../index.js';

const noColor: Color = (input) => String(input);
export const PlainPalette: Palette = {
  default: noColor,
  60: noColor,
  50: noColor,
  40: noColor,
  30: noColor,
  20: noColor,
  10: noColor,
  message: noColor,
  greyMessage: noColor,
};
